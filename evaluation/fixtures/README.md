# Evaluation Fixtures

Only target-owned synthetic metadata/event fixtures belong here unless a separately governed dataset admission explicitly permits otherwise. Every JSON fixture must carry provenance and must set project/runtime authority false.
