# DONOR-011 harvest plan

- Integrate the donor-supported DSP preprocessing semantics through target-owned, brand-neutral code.
- Keep exact 12-lead and 1-lead checkpoints as hashed external challengers; never commit weight bytes.
- Reject permissive checkpoint loading, silent NaN repair, and the defective donor resampling path.
- Keep tracked CSV derivatives outside the target until their data terms are independently cleared.
- Preserve PTB-XL/MIMIC labels as source labels only, never project gold.
- Require focused, full-regression, exact-SHA CI, independent verification, guarded promotion, and target-main CI before acceptance.
