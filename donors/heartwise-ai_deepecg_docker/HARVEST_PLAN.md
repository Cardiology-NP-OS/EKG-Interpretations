# DONOR-013 bounded harvest design and execution
The existing target challenger/evaluation flow lacks explicit, provenance-bound model probability comparison. Add a target-owned non-runtime utility and exact tests; retain all other source capabilities with terminal dispositions. Source analysis is evidence, not copied runtime code.

Completed: pin exact source/tree; inventory all 41 tracked assets; selectively inspect 34 exact source files; execute the dependency-light upstream tests and synthetic input probes; compare all 40 material capabilities; implement model agreement with test-first proof; wire tests into CI and edge coverage; retain code/model/data licensing gaps and associated fork rename.

Final verification and promotion are pending the user's requested Sol XHigh switch. Local tests do not establish independent verification. Model metadata does not count as a functional analyzer/interpreter. The contribution is an executable evaluation utility that supports real pipeline validation; donor-specific classifiers remain nonfunctional for target use until licensing, exact checkpoint, preprocessing, and validation evidence are supplied.

No donor code, weights, sample payloads, credentials, or diagnostic authority are imported. No published source claims become target performance.
