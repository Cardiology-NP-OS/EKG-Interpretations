# V12.1 Clinical-Control Candidate

This directory registers the uploaded `EKG_CHATGPT_PROJECT_V12_1_HARDENED.zip` as a **quarantined candidate layer**.

It is additive to the current EKG signal/engineering substrate. It does not replace or override:
- PTB-XL source-byte verification;
- WFDB decoding/calibration;
- signal inspection;
- signal-quality/QC;
- CI/source gates;
- independent-machine receipts;
- GitHub/Neon provenance.

## Candidate capabilities

The pack contains a clinical/control layer that the current repository does not yet activate:
- output contract/schema;
- adult clinical criteria registry;
- anesthesia/perioperative overlay;
- self-audit rubric and response templates;
- 59-pattern registry;
- phenotype-vs-diagnosis boundaries;
- deterministic calculation engine;
- image-quality protocol;
- mode router;
- reporting language;
- input-security rules;
- 30-failure-mode registry;
- 21-record source registry;
- structured-output guide.

## Activation rule

Nothing in this candidate directory is clinical runtime authority merely because it was uploaded or because its own runtime validator passes.

Activation requires:
1. exact-source import with SHA-256 matching `IMPORT_MANIFEST.json`;
2. structural/schema validation;
3. deterministic calculation-engine tests;
4. criteria/source traceability audit;
5. pattern-registry contract and contradiction tests;
6. phenotype/diagnosis boundary tests;
7. security/failure-mode tests;
8. independent-machine verification;
9. explicit governed promotion into the active clinical-control namespace.

Until then, current EKG output remains engineering evidence only and must not infer diagnostic accuracy from this pack.
